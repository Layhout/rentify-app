import 'package:flutter/material.dart';

class NewItemFormTab extends StatelessWidget {
  const NewItemFormTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
